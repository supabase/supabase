---
id: post
title: 'Creating'
description: 'Add new records to your database with Supabase'
---


import DummyData from '../common/DummyData.mdx'
import Collapsable from '../../src/components/Collapsable'
import CommonResponses from '../common/CommonResponses.mdx'
import FromFunction from '../common/FromFunction.mdx'
import CustomCodeBlock from '../../src/components/CustomCodeBlock'
import { postSingleJs, postBulkJs } from './getExamples'


<DummyData />


### Create a record

<CustomCodeBlock
  header="Creates a new city."
  js={postSingleJs}
  jsHighlight="{7-11}"
  response={`{
  "body": null,
  "status": 201,
  "statusCode": 201,
  "statusText": "Created"
}`}
/>

### Bulk create

<CustomCodeBlock
  header="Creates a multiple new cities."
  js={postBulkJs}
  jsHighlight="{7-12}"
  response={`{
  "body": null,
  "status": 201,
  "statusCode": 201,
  "statusText": "Created"
}`}
/>

## Reference

### `from()`
<FromFunction verb='post' />

### `insert()`
```js {3}
supabase
  .from(tableName)
  .insert(data, options?)
```

#### `data: array`
Array of rows of type `object` which contain information to be saved into the selected table.
 
#### `options: object?`  
@todo

---

## Responses

#### 201 Created
Successful

<CommonResponses />