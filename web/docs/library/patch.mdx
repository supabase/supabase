---
id: patch
title: 'Updating'
description: 'How to update records in your database with Supabase'
---

import Collapsable from '../../src/components/Collapsable'
import CommonResponses from '../common/_CommonResponses.mdx'
import CommonFilters from '../common/CommonFilters'
import FromFunction from '../common/_FromFunction.mdx'
import CustomCodeBlock from '../../src/components/CustomCodeBlock'
import {
  patchRecordJs,
  patchFilterJs,
  patchNotJs,
  patchMatchJs,
  patchOvaJs,
  patchOvrJs,
} from './examples/patch'

<DummyData />

### Update record

<CustomCodeBlock
  header="Updates information about a city."
  js={patchRecordJs}
  response={`{
  error: null,
  data: [
    { "name": "Middle Earth", "country_id": 554 },
  ]
}`}
/>

## Reference

### `from()`

<FromFunction verb="patch" />

### `update()`

```js {3}
supabase
  .from(tableName)
  .update(data)
```

It is to note that it is required to apply filters when using `.update()`. Not using filters would result in an error.

#### `data: object`

New record values.

---

## Filtering

### `filter()`

<CustomCodeBlock
  header="Shows how to use filter( ) with update( )"
  js={patchFilterJs}
  response={`{
  error: null,
  data: [
    { "name": "Mordor", "country_id": 250 },
  ]
}`}
/>

<br />

<CommonFilters filter="filter" />

### `not()`

<CustomCodeBlock
  header="Shows how to use not( ) with update( )"
  js={patchNotJs}
  response={`{
  error: null,
  data: [
    { "name": "Mordor", "country_id": 76 },
    { "name": "Mordor", "country_id": 156 },
    { "name": "Mordor", "country_id": 554 },
    { "name": "Mordor", "country_id": 556 },
    { "name": "Mordor", "country_id": 840 }
  ]
}`}
/>

<br />

<CommonFilters filter="not" />

### `match()`

<CustomCodeBlock
  header="Shows how to use match( ) with update( )"
  js={patchMatchJs}
  response={`{
  error: null,
  data: [
    { "name": "Mordor", "country_id": 156 },
  ]
}`}
/>

<br />

<CommonFilters filter="match" />




### `ova()`

<CustomCodeBlock
  header="Shows how to use ova( ) with update( )"
  js={patchOvrJs}
  response={`{
  error: null,
  data: [
    { "id": 76,
      "name": "Mordor",
      "population_range_millions": [205, 215]
      "main_exports": ["beans", "minerals"],
    },
    { "id": 556,
      "name": "Mordor",
      "population_range_millions": [185, 195]
      "main_exports": ["oil", "beans"],
    }
  ]
}`}
/>

<br />


<CommonFilters filter="ova" />

### `ovr()`

<CustomCodeBlock
  header="Shows how to use ovr( ) with update( )"
  js={patchOvrJs}
  response={`{
  error: null,
  data: [
    { "id": 76,
      "name": "Mordor",
      "population_range_millions": [205, 215]
      "main_exports": ["beans", "minerals"],
    },
    { "id": 556,
      "name": "Mordor",
      "population_range_millions": [185, 195]
      "main_exports": ["oil", "beans"],
    }
  ]
}`}
/>

<br />

<CommonFilters filter="ovr" />

