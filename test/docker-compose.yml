version: '3'
services:
  db:
    build: ./postgres
    ports:
      - "6543:5432"
    # env_file:
    #   - "./config/local/db.env"
    environment:
      POSTGRES_USER: supabase
      POSTGRES_PASSWORD: postgres
      POSTGRES_PORT: 5432
       