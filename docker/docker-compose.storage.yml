# Auto-generated by scripts/generate-compose-storage.py
# DO NOT EDIT — regenerate with: python3 scripts/generate-compose-storage.py

services:
  storage-default:
    container_name: supabase-storage-default
    image: supabase/storage-api:v1.22.25
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      ANON_KEY: ${ANON_KEY}
      SERVICE_KEY: ${SERVICE_ROLE_KEY}
      POSTGREST_URL: http://rest-default:3000
      PGRST_JWT_SECRET: ${JWT_SECRET}
      DATABASE_URL: "postgresql://supabase_storage_admin:${POSTGRES_PASSWORD}@db:5432/postgres"
      FILE_SIZE_LIMIT: ${FILE_SIZE_LIMIT:-52428800}
      STORAGE_BACKEND: ${STORAGE_BACKEND:-file}
      FILE_STORAGE_BACKEND_PATH: /var/lib/storage
      TENANT_ID: "default"
      GLOBAL_S3_BUCKET: ${STORAGE_S3_BUCKET:-supabase-storage}
      REGION: ${STORAGE_S3_REGION:-us-east-1}
      GLOBAL_S3_ENDPOINT: ${STORAGE_S3_ENDPOINT:-}
      AWS_ACCESS_KEY_ID: ${STORAGE_S3_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${STORAGE_S3_ACCESS_KEY:-}
      ENABLE_IMAGE_TRANSFORMATION: ${IMGPROXY_ENABLE_WEBP_DETECTION:-true}
      IMGPROXY_URL: http://imgproxy:5001
      PGSSLMODE: ${PGSSLMODE:-disable}
    volumes:
      - ./volumes/storage/default:/var/lib/storage:z
    networks:
      - supabase_network_default
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/status"]
      timeout: 5s
      interval: 5s
      retries: 3

  storage-project-alpha:
    container_name: supabase-storage-project-alpha
    image: supabase/storage-api:v1.22.25
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      ANON_KEY: ${DB_ALPHA_ANON_KEY}
      SERVICE_KEY: ${DB_ALPHA_SERVICE_KEY}
      POSTGREST_URL: http://rest-project-alpha:3000
      PGRST_JWT_SECRET: ${DB_ALPHA_JWT_SECRET}
      DATABASE_URL: "postgresql://supabase_storage_admin:${DB_ALPHA_PASSWORD}@db-project-alpha:5432/postgres"
      FILE_SIZE_LIMIT: ${FILE_SIZE_LIMIT:-52428800}
      STORAGE_BACKEND: ${STORAGE_BACKEND:-file}
      FILE_STORAGE_BACKEND_PATH: /var/lib/storage
      TENANT_ID: "project-alpha"
      GLOBAL_S3_BUCKET: ${STORAGE_S3_BUCKET:-supabase-storage}
      REGION: ${STORAGE_S3_REGION:-us-east-1}
      GLOBAL_S3_ENDPOINT: ${STORAGE_S3_ENDPOINT:-}
      AWS_ACCESS_KEY_ID: ${STORAGE_S3_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${STORAGE_S3_ACCESS_KEY:-}
      ENABLE_IMAGE_TRANSFORMATION: ${IMGPROXY_ENABLE_WEBP_DETECTION:-true}
      IMGPROXY_URL: http://imgproxy:5001
      PGSSLMODE: ${PGSSLMODE:-disable}
    volumes:
      - ./volumes/storage/project-alpha:/var/lib/storage:z
    networks:
      - supabase_network_default
      - supabase_network_project-alpha
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/status"]
      timeout: 5s
      interval: 5s
      retries: 3

  storage-project-beta:
    container_name: supabase-storage-project-beta
    image: supabase/storage-api:v1.22.25
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      ANON_KEY: ${DB_BETA_ANON_KEY}
      SERVICE_KEY: ${DB_BETA_SERVICE_KEY}
      POSTGREST_URL: http://rest-project-beta:3000
      PGRST_JWT_SECRET: ${DB_BETA_JWT_SECRET}
      DATABASE_URL: "postgresql://supabase_storage_admin:${DB_BETA_PASSWORD}@db-project-beta:5432/postgres"
      FILE_SIZE_LIMIT: ${FILE_SIZE_LIMIT:-52428800}
      STORAGE_BACKEND: ${STORAGE_BACKEND:-file}
      FILE_STORAGE_BACKEND_PATH: /var/lib/storage
      TENANT_ID: "project-beta"
      GLOBAL_S3_BUCKET: ${STORAGE_S3_BUCKET:-supabase-storage}
      REGION: ${STORAGE_S3_REGION:-us-east-1}
      GLOBAL_S3_ENDPOINT: ${STORAGE_S3_ENDPOINT:-}
      AWS_ACCESS_KEY_ID: ${STORAGE_S3_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${STORAGE_S3_ACCESS_KEY:-}
      ENABLE_IMAGE_TRANSFORMATION: ${IMGPROXY_ENABLE_WEBP_DETECTION:-true}
      IMGPROXY_URL: http://imgproxy:5001
      PGSSLMODE: ${PGSSLMODE:-disable}
    volumes:
      - ./volumes/storage/project-beta:/var/lib/storage:z
    networks:
      - supabase_network_default
      - supabase_network_project-beta
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/status"]
      timeout: 5s
      interval: 5s
      retries: 3

networks:
  supabase_network_default:
    external: true
    name: supabase_network_default
  supabase_network_project-alpha:
    external: true
    name: supabase_network_project-alpha
  supabase_network_project-beta:
    external: true
    name: supabase_network_project-beta

