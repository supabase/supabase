# Auto-generated by scripts/generate-compose-rest.py
# Do not edit manually - regenerate with: python3 scripts/generate-compose-rest.py
version: '3.8'
services:

  rest-default:
    container_name: supabase-rest-default
    image: postgrest/postgrest:v12.2.9
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      PGRST_DB_URI: "postgres://authenticator:${POSTGRES_PASSWORD}@db:5432/postgres"
      PGRST_DB_SCHEMAS: "${PGRST_DB_SCHEMAS:-public,graphql_public}"
      PGRST_DB_ANON_ROLE: anon
      PGRST_JWT_SECRET: ${JWT_SECRET}
      PGRST_DB_USE_LEGACY_GUCS: "false"
      PGRST_APP_SETTINGS_JWT_SECRET: ${JWT_SECRET}
      PGRST_APP_SETTINGS_JWT_EXP: "${JWT_EXPIRY:-3600}"
    command: "postgrest"
    networks:
      - supabase_network

  rest-project-alpha:
    container_name: supabase-rest-project-alpha
    image: postgrest/postgrest:v12.2.9
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      PGRST_DB_URI: "postgres://authenticator:${DB_ALPHA_PASSWORD}@db-alpha:5432/postgres"
      PGRST_DB_SCHEMAS: "${PGRST_DB_SCHEMAS:-public,graphql_public}"
      PGRST_DB_ANON_ROLE: anon
      PGRST_JWT_SECRET: ${DB_ALPHA_JWT_SECRET}
      PGRST_DB_USE_LEGACY_GUCS: "false"
      PGRST_APP_SETTINGS_JWT_SECRET: ${DB_ALPHA_JWT_SECRET}
      PGRST_APP_SETTINGS_JWT_EXP: "${JWT_EXPIRY:-3600}"
    command: "postgrest"
    networks:
      - supabase_network

  rest-project-beta:
    container_name: supabase-rest-project-beta
    image: postgrest/postgrest:v12.2.9
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      PGRST_DB_URI: "postgres://authenticator:${DB_BETA_PASSWORD}@db-beta:5432/postgres"
      PGRST_DB_SCHEMAS: "${PGRST_DB_SCHEMAS:-public,graphql_public}"
      PGRST_DB_ANON_ROLE: anon
      PGRST_JWT_SECRET: ${DB_BETA_JWT_SECRET}
      PGRST_DB_USE_LEGACY_GUCS: "false"
      PGRST_APP_SETTINGS_JWT_SECRET: ${DB_BETA_JWT_SECRET}
      PGRST_APP_SETTINGS_JWT_EXP: "${JWT_EXPIRY:-3600}"
    command: "postgrest"
    networks:
      - supabase_network

networks:
  supabase_network:
    external: true

