import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'supabase-ionic-vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
