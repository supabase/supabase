'use client'

/**
 * Placeholder for future observability-specific instrumentation.
 * Currently we just render nothing so the component can be safely included
 * in shared providers without additional wiring.
 */
const PageObservability = () => null

export { PageObservability }
