{
  "name": "templates",
  "version": "0.0.0",
  "description": "Supabase project composer templates and shadcn-compatible GitHub registry",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./node": "./node.ts"
  },
  "sideEffects": false,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "clean": "rimraf node_modules",
    "generate": "tsx scripts/generate.ts",
    "sync-registry": "tsx scripts/sync-registry.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run"
  },
  "dependencies": {
    "smol-toml": "^1.3.4"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsconfig": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "license": "MIT"
}
