{
  "id": "queues",
  "name": "Queues (pgmq)",
  "description": "pgmq extension with a default job queue for background processing",
  "category": "Database",
  "version": "1.0.0",
  "tags": [
    "queues",
    "pgmq",
    "background-jobs"
  ],
  "dependencies": {
    "required": [
      "database"
    ]
  }
}
