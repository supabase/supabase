{
  "id": "auth-email",
  "name": "Email Authentication",
  "description": "Email/password authentication with user profiles and RLS",
  "category": "Auth",
  "tags": ["auth", "profiles", "rls", "email"],
  "dependencies": {
    "required": ["auth", "database"]
  }
}
