{
  "id": "mcp-server",
  "name": "MCP Server",
  "description": "Edge Function MCP server with a tool registry for exposing project capabilities to MCP clients",
  "category": "AI",
  "version": "1.0.0",
  "tags": ["mcp", "ai", "tools", "functions"],
  "dependencies": {
    "required": ["database", "api", "functions"]
  }
}
