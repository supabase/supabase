{
  "id": "storage",
  "name": "Storage",
  "description": "S3-compatible object storage for files",
  "category": "Core",
  "tags": ["storage", "files", "s3"],
  "dependencies": {
    "required": ["database"]
  }
}
