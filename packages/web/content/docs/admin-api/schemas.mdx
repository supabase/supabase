export const metadata = {
  title: 'Schemas',
  description:
    'A database contains one or more named schemas, which in turn contain tables. Schemas also contain other kinds of named objects, including data types, functions, and operators.',
}

### Introduction

A database contains one or more named schemas, which in turn contain tables. Schemas also contain other kinds of named objects, including data types, functions, and operators. 
Read more about schemas [here](https://www.postgresql.org/docs/current/ddl-schemas.html).


### GET

Request a list of schemas

```
curl \
  https://fakeurl.com
```

### POST

Create a new schema

```
curl \
  https://fakeurl.com
```