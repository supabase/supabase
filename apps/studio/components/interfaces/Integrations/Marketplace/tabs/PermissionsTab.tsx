import { TabPlaceholder } from './TabPanel'

export const PermissionsTab = () => (
  <TabPlaceholder
    title="Permissions"
    description="OAuth scopes granted to this integration, and the objects it manages in your project. Visible once permission metadata is published from the marketplace."
  />
)
