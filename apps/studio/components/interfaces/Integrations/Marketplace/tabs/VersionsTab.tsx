import { TabPlaceholder } from './TabPanel'

export const VersionsTab = () => (
  <TabPlaceholder
    title="Versions"
    description="Changelog and version history for this integration. Available once the marketplace publishes release metadata."
  />
)
