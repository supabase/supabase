'use client'

import DefaultLayout from 'components/layouts/DefaultLayout'

export default function V2OrgSegmentLayout({ children }: { children: React.ReactNode }) {
  return <DefaultLayout>{children}</DefaultLayout>
}
