import { describe, it } from 'vitest'
// Tests for lib/api/self-hosted/provisioner.ts (Plan 01)
describe('getProvisioner', () => {
  it.todo('returns a SchemaProvisioner instance')
  it.todo('returns the same instance on repeated calls (singleton)')
  it.todo('throws if IS_PLATFORM is true')
})
