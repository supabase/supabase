import { describe, it } from 'vitest'
// Tests for pages/api/platform/projects/[ref]/rotate-keys.ts (Plan 02)
describe('POST /api/platform/projects/[ref]/rotate-keys', () => {
  it.todo('calls provisioner.rotateKeys with ref')
  it.todo('returns updated project with new keys')
  it.todo('returns 404 when project not found')
  it.todo('returns 500 on rotation failure')
})
