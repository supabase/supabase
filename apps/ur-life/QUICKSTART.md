# UR Life - 快速开始指南 🚀

5分钟内让你的 UR Life 应用运行起来！

---

## 📦 第一步：安装依赖

```bash
cd apps/ur-life
pnpm install
```

如果没有 pnpm：
```bash
npm install -g pnpm
```

---

## ☁️ 第二步：设置 Supabase

### 2.1 创建项目

1. 访问 [supabase.com](https://supabase.com)
2. 点击 "New Project"
3. 填写项目信息：
   - 名称: `ur-life`
   - 数据库密码: (保存好这个密码)
   - 区域: 选择最近的

### 2.2 运行数据库迁移

1. 打开 Supabase Dashboard
2. 点击左侧 "SQL Editor"
3. 复制 `supabase/migrations/20250104000000_create_ur_life_schema.sql` 的全部内容
4. 粘贴到 SQL Editor
5. 点击 "RUN" 运行

✅ 成功后你会看到 "Success. No rows returned"

### 2.3 获取 API 密钥

1. 点击左侧 "Settings" → "API"
2. 找到：
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public key**: `eyJhbGc...` (很长的字符串)

---

## 🔑 第三步：配置环境变量

```bash
cp .env.example .env.local
```

编辑 `.env.local`：

```env
VITE_SUPABASE_URL=https://你的项目id.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon-key
```

---

## 👥 第四步：创建测试用户

### 方式一：使用 Supabase Dashboard（推荐）

1. 打开 Authentication → Users
2. 点击 "Add user" → "Create new user"
3. 创建测试用户：

**用户 1 (Fox)**
- Email: `fox123@ur-life.app`
- Password: `rochester2025`
- User Metadata:
```json
{
  "net_id": "fox123",
  "name": "Fox",
  "major": "Computer Science",
  "year": "Junior"
}
```

**用户 2 (Bear)**
- Email: `bear456@ur-life.app`
- Password: `yellowjacket`
- User Metadata:
```json
{
  "net_id": "bear456",
  "name": "Bear",
  "major": "Mathematics",
  "year": "Senior"
}
```

**用户 3 (Cat)**
- Email: `cat789@ur-life.app`
- Password: `meowmeow123`
- User Metadata:
```json
{
  "net_id": "cat789",
  "name": "Cat",
  "major": "Biology",
  "year": "Sophomore"
}
```

### 方式二：使用注册功能

运行应用后，你也可以直接注册新用户（如果你实现了注册功能）。

---

## 🚀 第五步：启动应用

```bash
pnpm dev
```

浏览器会自动打开 `http://localhost:3000`

---

## 🎉 开始使用

### 登录

1. 点击任一 Demo 账户卡片自动填充
2. 或手动输入：
   - Net ID: `fox123`
   - Password: `rochester2025`
3. 点击 "Sign In"

### 主要功能

#### 📝 任务管理（Home 页面）
- 输入框添加任务
- 勾选完成任务
- 查看任务历史
- 恢复已完成任务

#### 👤 个人资料（Profile 页面）
- 更换头像
- 编辑个人信息
- 添加课程到日历
- 管理联系人列表

#### 🎓 学位进度（Degree Tracker 页面）
- 勾选已完成课程
- 查看完成百分比
- 跟踪各类别进度

---

## 🔍 验证安装

### 检查清单

✅ Supabase 项目已创建
✅ 数据库迁移已运行
✅ 环境变量已配置
✅ 测试用户已创建
✅ 应用可以启动
✅ 可以成功登录
✅ 可以添加任务

---

## 🐛 遇到问题？

### 问题 1: "Invalid API key"

**解决方案**:
- 检查 `.env.local` 文件是否存在
- 确认 API 密钥没有多余的空格或引号
- 重启开发服务器 (Ctrl+C 然后 `pnpm dev`)

### 问题 2: "Failed to fetch"

**解决方案**:
- 确认 Supabase 项目 URL 正确
- 检查网络连接
- 在 Supabase Dashboard 检查项目状态

### 问题 3: 登录失败

**解决方案**:
- 确认用户已在 Supabase Dashboard 中创建
- 检查 User Metadata 格式是否正确
- 尝试在 SQL Editor 运行：
```sql
SELECT * FROM auth.users;
```

### 问题 4: 数据库表不存在

**解决方案**:
- 重新运行迁移 SQL
- 检查 SQL Editor 是否显示错误
- 在 Database → Tables 中确认表已创建

### 问题 5: CORS 错误

**解决方案**:
- 在 Authentication → URL Configuration
- 添加 `http://localhost:3000` 到 Site URL
- 添加 `http://localhost:3000/**` 到 Redirect URLs

---

## 📚 下一步

现在你的应用已经运行了！

### 学习更多
- 阅读 [README.md](README.md) 了解详细功能
- 查看 [DEPLOYMENT.md](DEPLOYMENT.md) 学习部署
- 探索 `src/lib/supabase.js` 了解 API

### 自定义
- 修改 `src/styles/` 中的样式
- 在 `src/js/` 中添加新功能
- 扩展数据库模式

### 部署
- 部署到 Vercel: `vercel --prod`
- 部署到 Netlify: `netlify deploy --prod`

---

## 🎯 测试功能清单

试试这些功能确保一切正常：

### 任务管理
- [ ] 添加一个任务
- [ ] 完成一个任务
- [ ] 查看任务历史
- [ ] 恢复一个任务

### 个人资料
- [ ] 更换头像
- [ ] 编辑个人信息
- [ ] 修改密码

### 课程日历
- [ ] 添加一门课程
- [ ] 查看课程日历
- [ ] 删除一门课程

### 联系人
- [ ] 添加一个联系人
- [ ] 按类别查看
- [ ] 发送邮件（点击邮箱）

### 学位进度
- [ ] 勾选一门课程
- [ ] 查看进度更新
- [ ] 检查总体进度

---

## 💡 小贴士

1. **开发工具**: 按 F12 打开浏览器开发者工具查看控制台
2. **热重载**: 修改代码后页面会自动刷新
3. **数据持久化**: 所有数据都保存在 Supabase 云端
4. **多设备**: 登录同一账户可在多个设备同步

---

## 🎓 学习资源

- [Supabase 文档](https://supabase.com/docs)
- [Vite 文档](https://vitejs.dev/)
- [JavaScript ES6+](https://developer.mozilla.org/en-US/docs/Web/JavaScript)

---

<p align="center">
  <strong>🎉 恭喜！你的 UR Life 应用已经运行了！</strong><br>
  <em>Have fun building! 🚀</em>
</p>
