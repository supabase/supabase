---
title: Expandable Alert
description: An alert-styled block with a collapsible content section, toggled by an icon button.
fragment: true
---

Expandable Alert combines an Alert-style container with a collapsible content section. Use it when the callout itself needs to stay visible while optional detail can be expanded.

Use Expandable Alert when the disclosure is itself a callout. It owns the alert styling, padding, trigger treatment, and toggle button. Use [Collapsible](../components/collapsible) instead when you only need generic reveal and hide behaviour and the surrounding layout is not an alert.

For non-expandable product callouts, use [Admonition](./admonition). Use the low-level [Alert](../components/alert) primitive only when a wrapper or bespoke composition needs direct control over the alert structure.

<ComponentPreview name="expandable-alert-demo" peekCode wide />

## Usage

```tsx
import { ExpandableAlert } from 'ui-patterns/expandable-alert'
```

```tsx
<ExpandableAlert trigger="Need help?">
  <p>Content revealed when expanded.</p>
</ExpandableAlert>
```

### Default open

```tsx
<ExpandableAlert trigger="Details" defaultOpen>
  <p>This section is expanded by default.</p>
</ExpandableAlert>
```

### Variants

Supports the same `variant` prop as Alert: `default`, `destructive`, and `warning`.

```tsx
<ExpandableAlert trigger="Warning" variant="warning">
  <p>Something needs attention.</p>
</ExpandableAlert>
```
