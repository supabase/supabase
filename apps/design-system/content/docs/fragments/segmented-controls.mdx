---
title: Segmented controls
description: Switch between views or narrow a list, without leaving the page.
---

A segmented control is a small row of mutually exclusive options in a single bordered container.
It is the `segmented` variant of [Toggle Group](../components/toggle-group) — there is no separate
component, and props are documented on that page.

Reach for it when the choice is small, always has an answer, and changes what is on screen right
now. It is not a form field.

## Sanctioned uses

Both are single-select, and both set `allowDeselect={false}`.

### View state

Which representation of the same thing am I looking at. Data or Definition in the table editor,
Table or Chart for a query result. Usually two items.

<ComponentPreview name="segmented-control-view-state" peekCode />

### Filter

Narrowing a list to a subset. Always include an explicit **All** segment as the unfiltered state,
and make it the default. Usually three items.

<ComponentPreview name="segmented-control-filter" peekCode />

## Rules

### Never use deselection to mean "no filter"

Clicking the active segment to clear it is invisible — nothing in the control tells the user that
state exists, and the result is a control with no selection at all. Add an **All** segment and set
`allowDeselect={false}`.

| Bad                                       | Good                                                  |
| ----------------------------------------- | ----------------------------------------------------- |
| `Active \| Revoked`, deselect to see both | `All \| Active \| Revoked`, `All` selected by default |

### Two to four items

Past four, the row gets too wide to scan and starts competing with the content it filters. Use a
[Select](../components/select) instead.

### Not for form fields

Segmented controls are for display state and filters — things that take effect immediately and are
not saved. If the choice is persisted:

| Choice                            | Use                                      |
| --------------------------------- | ---------------------------------------- |
| A saved boolean setting           | [Switch](../components/switch)           |
| A saved mutually exclusive choice | [Radio Group](../components/radio-group) |
| Display state or a filter         | Segmented control                        |

The distinction is whether the control mutates state. A segmented control changes what you are
looking at; a form control changes what is stored. Putting one in a form also breaks the
expectation that nothing is written until submit.

### Not a replacement for Tabs

If the options swap between substantial panels of content — different forms, different tables,
sections with their own headings — use [Tabs](../components/tabs). Tabs carry the right semantics
for that, and they have room for the longer labels those sections need.

Segmented controls suit a compact switch sitting next to or inside the thing it affects, like a
table footer or a toolbar.

## Usage

```tsx
import { ToggleGroup, ToggleGroupIndicator, ToggleGroupItem } from 'ui'
;<ToggleGroup
  type="single"
  variant="segmented"
  size="tiny"
  value={status}
  onValueChange={setStatus}
  allowDeselect={false}
  aria-label="Filter keys by status"
>
  <ToggleGroupIndicator />
  <ToggleGroupItem value="all">All</ToggleGroupItem>
  <ToggleGroupItem value="active">Active</ToggleGroupItem>
  <ToggleGroupItem value="revoked">Revoked</ToggleGroupItem>
</ToggleGroup>
```

`ToggleGroupIndicator` slides the selected background between segments, the same way
[Tabs](../components/tabs) animates its underline. Include it by default — the movement is what
tells the user the segments are one control rather than three buttons. Drop it only where the
control mounts already-changed, such as inside a popover that opens on the selected value.

Always give the root an `aria-label` describing what is being switched or filtered — the segments
name the options, not the axis.

Segments hug their labels. If you need them evenly sized, add `className="flex-1"` to each item and
a width to the root.
