import React from 'react'
import { UserData } from '../Ticket/hooks/use-conf-data'

import { TicketBrick } from './TicketBrick'
import styles from './brick-wall.module.css'

type user = UserData

interface Props {
  users: user[]
}

export default function TicketBrickWall({ users }: Props) {
  return (
    <div className="relative">
      <div
        className={[
          'absolute top-[196px] w-[475px] h-[420px] z-10 left-[-130px] rotate-90',
          styles['brick-wall-mask--left'],
        ].join(' ')}
      ></div>
      <div
        className={[
          styles['brick-wall-mask--right'],
          'absolute top-[196px] w-[475px] h-[420px] z-10 right-[-130px] -rotate-90',
        ].join(' ')}
      ></div>

      <div className="mx-auto overflow-hidden bg-[#1C1C1C]">
        <div className="max-w-[38rem] mx-auto mt-24">
          <h2 className="radial-gradient-text-scale-500 text-center">
            Explore the unique tickets generated by our community
          </h2>
        </div>
        <div className="grid gap-4 -ml-48 -mr-48 my-12 relative">
          <div className="flex gap-4 justify-center">
            {users.slice(0, 4).map((user) => (
              <TicketBrick key={user.username} user={user} />
            ))}
          </div>
          <div className="flex gap-4 justify-center ml-36">
            {users.slice(4, 8).map((user) => (
              <TicketBrick key={user.username} user={user} />
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
