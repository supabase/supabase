// public/markdown/manifest.json is generated by scripts/build-markdown.ts
// and gitignored, so it may not exist on disk when tsc runs.
declare module '@/public/markdown/manifest.json' {
  const slugs: string[]
  export = slugs
}
