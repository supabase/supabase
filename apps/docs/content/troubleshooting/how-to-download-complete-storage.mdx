---
title = "How to download complete Supabase Storage"
date_created = "2026-04-14T00:00:00+00:00"
topics = [ "storage" ]
keywords = [ "storage", "download", "backup", "export", "s3", "rclone", "aws cli", "dump", "storage.objects" ]
---

Supabase Storage consists of two parts that must be backed up separately:

- **File metadata** — stored in the `storage.objects` table in your Postgres database
- **Actual files** — stored in Supabase's S3-compatible storage backend

### Step 1: Export file metadata (via database dump)

Run the following three CLI commands to dump your full database, which includes `storage.objects`:
```
supabase db dump --db-url [CONNECTION_STRING] -f roles.sql --role-only
supabase db dump --db-url [CONNECTION_STRING] -f schema.sql
supabase db dump --db-url [CONNECTION_STRING] -f data.sql --use-copy --data-only
```
Your `CONNECTION_STRING` can be found in [Dashboard Database Settings](/dashboard/project/_/database/settings?showConnect=true).
See the [backup restore guide](/docs/guides/platform/migrating-within-supabase/backup-restore) for full details.

### Step 2: Download actual files (via S3-compatible endpoint)

1. Go to **Dashboard → Storage → [S3 Configuration](/dashboard/project/_/storage/s3)**
2. Generate an access key + secret key pair (shown once — save it immediately)
3. Use any S3-compatible tool (AWS CLI, rclone, Cyberduck, etc.) to sync your files locally using the endpoint and credentials from the dashboard

See the [S3 authentication doc](/docs/guides/storage/s3/authentication) for connection details.

> **Note:** Repeat Step 2 for each bucket. Both steps are required for a complete backup — the database dump alone won't include the actual files, and the S3 sync alone won't include the metadata.
