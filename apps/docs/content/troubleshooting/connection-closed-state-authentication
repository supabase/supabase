---
title = "Connection closed when state was authentication"
topics = ["database", "supavisor"]
keywords = ["connections", "pooler", "authentication"]

[[errors]]
message = "Connection closed when state was authentication"
---

This error happens when either the database doesn’t exist or if the user doesn't have the right credentials.
