---
title: 'Server-Side Rendering'
subtitle: 'How SSR works with Supabase Auth.'
---

SSR frameworks move rendering and data fetches to the server, to reduce client bundle size and execution time.

Supabase Auth is fully compatible with SSR. You need to make a few changes to the configuration of your Supabase client.

## Client-side vs. SSR

## Using Auth with SSR

We recommend using the helper package [`@supabase/ssr`](https://www.npmjs.com/package/@supabase/ssr).
