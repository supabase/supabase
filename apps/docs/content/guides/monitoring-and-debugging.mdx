---
title: Observability
description: 'Observe project data, detect and resolve issues, hire an agent, or export to your own stack.'
---

<AiPrompt id="monitoring-and-debugging" />

Monitor your Supabase project with the tools you already use, as a person or an agent.

## 1. Observe the data

The sources you can query, and where to read them.

<ContentListings id="telemetry-access-what" />

## 2. Detect and resolve issues

Use those sources to pick up a signal. If you already have a named error, diagnose it.

<ContentListings id="telemetry-detect-resolve" />

## 3. Hire an agent

Set up a read-only agent in your harness and run it on a schedule.

<ContentListings id="telemetry-hire-agent" />

## 4. Export

Send logs and traces to the tools you already run.

<ContentListings id="telemetry-export" />
