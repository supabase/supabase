import { NavMenuProvider } from '~/components/Navigation/NavigationMenu/NavigationMenuContext'
import { MenuId } from '~/components/Navigation/NavigationMenu/menus'
import Layout from '~/layouts/DefaultGuideLayout'

export const meta = {
  id: '404',
  title: '404 not found',
  description: '404 not found',
}

404 not found

export const Page = ({ children }) => (
  <NavMenuProvider menuId={MenuId.Home}>
    <Layout meta={meta} children={children} />
  </NavMenuProvider>
)

export default Page
