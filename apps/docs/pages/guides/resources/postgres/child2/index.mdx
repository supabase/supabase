import Layout from '~/layouts/DefaultGuideLayout'

export const meta = {
  title: 'Managing a table',
  description:
    'Resources to help you get up to speed with Postgres, the database powering Supabase.',
}

managing a table

export const Page = ({ children }) => <Layout meta={meta} children={children} />

export default Page
