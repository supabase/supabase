import Layout from '~/layouts/DefaultGuideLayout'

export const meta = {
  id: 'client-libs',
  title: 'Client Libraries',
  description: 'Suapbase provides several client libraries for the REST interface.',
  video: 'https://www.youtube.com/v/7CqlTU9aOR4',
}

A doc on the Client Libraries, how they are structured, and where to find the docs.

export const Page = ({ children }) => <Layout meta={meta} children={children} />

export default Page
