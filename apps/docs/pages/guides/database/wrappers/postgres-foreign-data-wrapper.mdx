import Layout from '~/layouts/DefaultGuideLayout'

export const meta = {
  id: 'wrappers-postgres',
  title: 'Connecting to Postgres',
  description: 'Creating Foreign Tables from a remote Postgres database.',
  subtitle: 'Creating Foreign Tables from a remote Postgres database.',
}

Wrappers with Stripe

export const Page = ({ children }) => <Layout meta={meta} children={children} />

export default Page
