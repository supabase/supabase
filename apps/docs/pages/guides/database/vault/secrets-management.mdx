import Layout from '~/layouts/DefaultGuideLayout'

export const meta = {
  title: 'Secrets Management',
  description: 'Storing secrets, environment variables, and encryption keys.',
}

The Vault can be used to store secrets and encryption keys. It is enabled by default for all projects created after December 2020.

export const Page = ({ children }) => <Layout meta={meta} children={children} />

export default Page
