import Layout from '~/layouts/DefaultGuideLayout'

export const meta = {
  title: 'Vault',
  description: 'Secrets Management and Data Encryption.',
}

Supabase Vault provides two important pieces of functionality:

- Secret Management: A pre-configured table where you can store secrets securely by encrypting them when they are written to disk.
- Data Encryption: protect your sensitive data by using encryption on your own data columns in a flexible way.

export const Page = ({ children }) => <Layout meta={meta} children={children} />

export default Page
